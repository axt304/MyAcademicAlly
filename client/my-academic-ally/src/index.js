import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Import your global CSS styles here if needed
import App from './App'; // Import your root component (e.g., App)

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

